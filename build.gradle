apply plugin: 'java'
apply plugin: 'eclipse'

repositories {
	jcenter()
}

dependencies {
	compile group: 'org.apache.commons', name : 'commons-lang3', version: '3.6'
}

sourceSets {
	main {
		java {
			srcDir 'src'
		}
	}
}